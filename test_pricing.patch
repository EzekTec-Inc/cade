--- crates/cade-ai/src/registry.rs
+++ crates/cade-ai/src/registry.rs
@@ -155,6 +155,14 @@
         assert!(p.output > 0.0);
     }
 
+    #[test]
+    fn pricing_gpt_5_5() {
+        let registry = ModelRegistry::new();
+        let p = registry.pricing_for_model("openai/gpt-5.5");
+        assert_eq!(p.input, 5.0);
+        assert_eq!(p.output, 30.0);
+    }
+
     #[test]
     fn pricing_gemini_25_pro() {
         let registry = ModelRegistry::new();
