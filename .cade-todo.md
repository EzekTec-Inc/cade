# TUI Parity M15-M18

## M15: Slash Command Palette
- [x] Create palette.rs (pure: command defs, fuzzy match, parser)
- [x] Add palette state to session.rs (open/close, query, selection)
- [x] Add Ctrl+K + '/' trigger to app.rs
- [x] Render palette overlay in app.rs
- [x] Implement command actions: /logout, /new, /clear, /copy, /help, /unknown (server-dependent cmds show "coming soon" toast)
- [x] Deferred to M16–M18: /model (HTTP PATCH), /search, /memory, /agents, /context, /stats, /artifacts, /checkpoints, /skills, /mcp
- [x] Tests for palette.rs (18), session palette methods (11), session helpers (2)

## M16: Memory + Agent Config Panel
- [x] Add memory overlay state to session.rs
- [x] HTTP: GET /v1/agents/:id/memory, PUT memory block, PATCH agent config (model)
- [x] Render memory panel as overlay (list labels + content, editable)
- [x] Agent info panel in sidebar (model, provider, short id)
- [x] Dirty-state indicator + Save-button disabled when clean
- [x] Save-success toast + per-overlay error banner
- [x] Keyboard: Esc closes memory overlay, Ctrl+S saves dirty block
- [x] Metrics display (deferred — server /metrics endpoint exists but not wired)

## M17: Checkpoints + Artifacts Browser
- [x] Add checkpoint overlay state to session.rs
- [x] Add artifact overlay state to session.rs
- [x] HTTP: GET/POST/DELETE checkpoints, artifacts, GET artifact detail, POST restore
- [x] Wire /checkpoints and /artifacts slash-commands in palette dispatch
- [x] Render checkpoint list overlay with restore/delete buttons
- [x] Render artifacts list overlay with detail pane + delete

## M18: Skills + MCP + Permissions
- [x] AgentTool type + tools_url/parse_tools + Question/QuestionOption/parse_ask_question (api.rs, +11 tests)
- [x] get_tools HTTP helper (http_wasm.rs)
- [x] Tools overlay state + question widget state in session.rs (+14 tests)
- [x] on_stream_tool_call detects ask_user_question and activates widget
- [x] Wire /skills + /mcp palette cmds → tools overlay
- [x] spawn_fetch_tools; AppAction variants (CloseToolsOverlay, AnswerQuestion, DismissQuestion, MoveQuestionCursor, ToggleQuestionChecked)
- [x] render_tools_overlay — read-only MCP tool list
- [x] render_question_widget — inline ask_user_question responder

## Infrastructure
- [x] Fix /dashboard/ trailing-slash (DONE — in mod.rs)
- [x] Rebuild trunk + cade-server release
- [x] Run all tests, clippy clean
