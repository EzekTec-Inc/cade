# Code Review Technical Debt Remediation Plan

## Phase 1: SQLite Migration Numbering Cleanup (Low Complexity)
*Status: Completed*
- Replaced the chaotic, monolithic `run_migrations` block with a structured `user_version` PRAGMA approach in `crates/cade-server/src/server/storage/sqlite/mod.rs`.
- `apply_schema` now cleanly builds the latest database structure for new databases.
- Legacy migrations run sequentially only if `user_version < 1`.

## Phase 2: Poison-Handling Regression Tests (Medium Complexity)
*Status: Completed*
- Added `test_db_lock_poisoning_yields_500` in `crates/cade-server/src/server/api/evals_test.rs`.
- Validated that intentionally poisoning the `AppState.db` mutex correctly results in the API returning an `HTTP 500` JSON response containing the text "db lock poisoned" without crashing the test runner.

## Phase 3: Modularize Oversized CLI Repl (High Complexity, High Value)
*Status: In Progress*
- Extract `SessionStats` into `stats.rs`.
- Extract `SlashCmd` and `parse_slash_with_skills` into `slash.rs`.
- Move interactive pickers from `mod.rs` into `pickers.rs`.