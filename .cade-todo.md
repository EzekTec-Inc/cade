## Plan: Modernize Turn Status Row

### 1. Track Tool Call Actions
- In `crates/cade-cli/src/cli/repl/mod.rs` (or within the `stream_turn` logic in `turn_loop.rs`), we need a way to track the number of reads, edits, and shell commands executed *during a single turn*.
- Currently, CADE tracks global session tool calls in `SessionStats` or similar. We will add a temporary tracker struct (e.g. `TurnStats`) inside `turn_loop.rs` that increments counters for `reads`, `edits`, `cmds` based on the tool calls dispatched during `dispatch_tool_calls()`. 

### 2. Update the `last_status` Formatting (Option A & C mix)
- In `crates/cade-cli/src/cli/repl/turn_loop.rs` at the end of the `agent_turn()` block (around line 550), replace the `summary` string formatting.
- **Drop the tokens:** Remove `in_delta` and `out_delta` formatting.
- **Add tool metrics:** Construct a string based on `TurnStats`. 
  - If 0 tools: `✓ Finished in {time}`
  - If tools were used: `✓ Finished in {time}  ·  {X} files read  ·  {Y} edited  ·  {Z} commands` (filtering out zero values).
- **Outcome Status:** Ensure the status icon reflects success (`✓`). If we abort early via Ctrl+C, the cancel path currently pushes an `ErrorMsg` into the timeline, but we can also set the status bar to `⚠ Interrupted after {time}`.

### 3. Cleanup TUI App Model
- In `crates/cade-tui/src/app/mod.rs`, ensure that the `status_style` colors map correctly to these states (the current code already uses `colors.success` for the existing text, so `✓ Finished...` will look great right out of the box).