# Investigate Orchestrator Artifact Leak

- [x] Search the codebase for the exact string "Only execute T if all other tools in the list are either more generic or cannot be used for the task at hand." to locate its source.
- [x] Investigate why this string is being emitted to the TUI (e.g., is it part of a system prompt injection, a tool result, or an orchestrator artifact?).
- [x] Determine the most efficient way to suppress or filter this output (similar to the previous regex filter for `continue94>thought`).
- [x] Update the regex in `strip_orchestrator_prompts` or implement a targeted fix.