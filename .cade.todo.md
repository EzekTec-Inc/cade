# TUI Refactoring: OpenCode Parity

- [ ] **Phase 1: Input Component Modernization**
  - Add `tui-textarea` dependency to `crates/cade-tui/Cargo.toml`.
  - Replace custom `Editor` logic in `crates/cade-tui/src/editor.rs` with `tui-textarea`.
  - Wire up key event delegation so `tui-textarea` handles its own cursor movement, multiline wrapping, and undo/redo.

- [ ] **Phase 2: Event & Render Loop Decoupling**
  - Audit `crates/cade-cli/src/cli/repl/turn_loop.rs` and `mod.rs`.
  - Ensure the TUI render tick runs independently of LLM streaming and tool execution blocks, utilizing `tokio::sync::mpsc` channels or `tokio::select!` for non-blocking UI redraws.

- [x] **Phase 3: Fluid Layout Refactor**
  - Update `crates/cade-tui/src/app/mod.rs` rendering logic.
  - Remove fragile `Constraint::Length` math.
  - Implement fluid layout using `Constraint::Fill`, `Constraint::Min`, and `Constraint::Percentage` to prevent jitter on terminal resize.

- [ ] **Phase 4: Aesthetic Theming Parity**
  - Expand the `.tmTheme` parser (`crates/cade-core/src/resources/themes.rs`).
  - Apply extracted palette colors (backgrounds, borders, accents) to all `Block`, `Border`, and `Badge` components across `cade-tui` to achieve a seamless, GUI-like appearance.
